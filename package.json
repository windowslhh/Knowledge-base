{
  "name": "gbrain",
  "version": "0.1.0",
  "type": "module",
  "description": "LLM knowledge base management system with Obsidian integration",
  "main": "src/cli/index.ts",
  "bin": {
    "gbrain": "bin/gbrain"
  },
  "scripts": {
    "dev": "bun run src/cli/index.ts",
    "build": "bun build src/cli/index.ts --compile --outfile bin/gbrain",
    "serve": "bun run src/mcp/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "gray-matter": "^4.0.3",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
