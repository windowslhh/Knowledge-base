# Agent 工作流定义

## 摄入 Agent (Ingest)

**触发**: 用户提供新的来源文档
**流程**:
1. 读取 `wiki/index.md` 了解现有知识结构
2. 分析源文档，提取关键实体和事实
3. 对每个实体：`read` 现有页面 → 合并新知识 → `write` 更新
4. 添加时间线条目记录来源
5. 更新 `[[wiki-link]]` 和标签
6. `ingest` 存储原始数据

## 查询 Agent (Query)

**触发**: 用户提出问题
**流程**:
1. `search` 或 `query` 查找相关页面
2. `read` 前 N 个结果页面
3. 综合所有信息回答问题
4. 引用来源页面 `[[slug]]`

## 维护 Agent (Maintain)

**触发**: 定期执行或用户请求
**流程**:
1. `list` 所有页面
2. 检查过期信息、缺失链接、孤立页面
3. 更新 compiled_truth
4. `sync` 确保 Obsidian 同步

## 丰富 Agent (Enrich)

**触发**: 新页面创建后
**流程**:
1. `read` 新页面
2. `search` 查找相关页面
3. 在相关页面间添加交叉引用 `[[wiki-link]]`
4. 补充共同标签
5. `backlinks` 验证链接完整性

## 简报 Agent (Briefing)

**触发**: 用户请求某主题概要
**流程**:
1. `query` 搜索主题
2. `read` 所有相关页面 + 时间线
3. 生成结构化简报（按时间线排列，引用来源）
